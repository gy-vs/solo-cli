# 宿主机上 solo-coder 的绝对路径（含 prompt.md、workspace/、出题/）
CODER_ROOT=/Users/gaoyong/solo-coder-0908

# 本项目在宿主机上的绝对路径。质检要把轨迹与桥接脚本挂进 solo-qa 容器，
# docker -v 的源路径由宿主 daemon 解析，不能用后端容器内的 /data。
SOLO_CLI_ROOT=/Users/gaoyong/solo-cli

# solo-qa 项目路径（宿主机）。质检与查重复用它的源码与 .env，只读挂载。
QA_PROJECT_HOST=/Users/gaoyong/solo-qa-0908

# Cursor skills 目录。出题规则的唯一来源是这里的 solo-prompt skill：后端把它挂进容器，
# 再链到 CLI 认的 ~/.cursor/skills，由 Cursor CLI 以 /solo-prompt 调起。
SKILL_DIR=/Users/gaoyong/.cursor/skills

# GitHub Token：出题要建仓库、推初始快照，跨设备题库也用它读写私有仓库。
# 本机执行 gh auth token 取值
GH_TOKEN=

# 跨设备题库（题目全集 + 领取归属 + 查重池）在设置页配置：启用开关、仓库、本机标识。
# 不走环境变量 —— 本机标识两台设备不能重名，而领取归属是按它记的，
# 放进 .env 容易连同项目一起复制过去，两台机器就会顶着同一个标识互相抢题

# 控制台对外端口
HOST_PORT=8788

# 可选：首次启动时作为设置页的初始值写入加密存储（之后以设置页为准，环境变量不再覆盖）
# Cursor：在 https://cursor.com/dashboard/api 创建 User API Key
CURSOR_API_KEY=
CURSOR_MODEL=claude-opus-5-thinking-high
# Claude Code 网关 Key（注入容器 apikey）
CC_API_KEY=
# solo-qa 身份：浏览器 Cookie 中的 solo_qa_session 与 solo_qa_csrf
QA_SESSION_COOKIE=
QA_CSRF_TOKEN=
