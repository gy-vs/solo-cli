# 已提交题目的数据订正

把已提交的题目逐条拿回轨迹里核过一遍之后，这些题的 GSB 理由里有与轨迹矛盾的断言。
矛盾集中在一类说法上：称某一侧「没跑测试」或「戛然而止」，而那一侧的轨迹末尾摆着成功的测试记录和完整收尾总结。

只收已提交状态、且问题出在理由里的题目。理由是提交给平台的判断正文，写错就是错报；与它一同提交的备注字段写的是「这些现象我看到了但没计入判断」，不在这份文档的范围内，库里也没有改动过。

成因是分析器喂给模型的材料被截断。轨迹步骤超过上限就压缩，而等距抽样那步会把尾部整段切掉，模型因此看不到收尾；压缩后的步骤又只带命令与是否报错、不带命令输出，测试跑出来是绿还是红，材料里本就分辨不出。截断已修，回归测试锁住了收尾步不许再被切掉。

下面分两类，区别在于错的那句话是不是判断的立足点。

第一类，错的是次要的一句，判断另有立足点——代码里一处能独立核到的缺陷，所以换掉那句话结论不动，正文已订正，每道题都给出改前改后两版。

第二类，错的那句就是判断的立足点，删掉它结论也就没了支撑。这类题**没有改后版本**，不是漏抄：改一句话要么得换结论，要么得另编一条理由，两样都不能由人在文本上替模型定，只能拿修好的材料重跑分析。这类题的小节里列了三栏——错的那句、判断落在哪一句、删掉之后还剩什么——便于复核。

理由已订正 4 道：26、33、112-gaoyong、120-gaoyong

理由待重跑 2 道：16、104-gaoyong

---

## 16　平台提交 ID 5917

- 结论：B 更好
- 上传时间：2026-09-22 02:09:27.810911
- 题目类型：0-1 代码生成　难度：困难　语言：TypeScript, Node.js
- 仓库：https://github.com/gy-vs/deflate-codec-core
- 处理：未改动，需重跑分析

### prompt 原文

> 我在做一个日志归档服务，每天的日志打成一个 gzip 文件存到对象存储，单个文件 2 到 8 GB。现在查一条日志要把整个文件拉下来从头解压，一次查询要等七八分钟。我想在压缩的时候就留下可以跳转的位置，查的时候只解压需要的那一段，同时产物还得是标准的 gzip，因为下游有别的团队直接用 gunzip 处理它。这次先只做编解码库，命令行入口和服务端都不做。
>
> 压缩这边要自己实现 DEFLATE。滑动窗口匹配、游程与距离的编码、哈夫曼码表的构造都要自己写，存储块、固定码表块、动态码表块三种块类型都要能产出，具体每一块用哪种由实现自己判断。外层封装成 gzip 容器，头部字段、CRC32 校验和末尾的原始长度都要正确。
>
> 解压这边要能吃下任意合法的 DEFLATE 流，不只是自己压出来的那种。我会拿 Node 自带的 zlib 用 1 到 9 各个压缩级别压同一批数据，再用这个库解开，结果要和原始数据逐字节相同。反过来，这个库压出来的东西用 zlib 解开也要逐字节相同。位流不是按字节对齐的，块与块交界处的处理我遇到过别的实现出错，这里要覆盖到。
>
> 可跳转这部分是我要的重点。压缩时每累计一定量的原始字节就强制收尾当前块、重置滑动窗口另起一块，同时输出一份索引，记录每个跳转点对应的原始偏移和它在压缩流里的位偏移。给定索引和一个原始偏移区间，要能只解压覆盖这个区间的那几块。重置窗口会让压缩率变差，所以跳转点的间隔要能配置，我需要一组数据来看不同间隔下压缩率和跳转粒度的取舍。
>
> 压缩率上，在我给的日志语料上，不设跳转点时的压缩后体积不能比 zlib 第 6 级大出 5% 以上。速度我不苛求，但压 1 GB 的数据不能跑到十分钟以上。
>
> 技术上有几点要求。用 TypeScript 写，跑在 Node 20 上，测试用 Node 自带的 node:test，不要引 jest 或 vitest。不要引 pako、fflate、zlib-js、browserify-zlib 这类现成的压缩库。node:zlib 只能出现在测试代码里当对照，实现代码里不能调用它，调用了这题就没意义了。交付成一个库，对外暴露压缩、解压、带索引压缩、按区间解压这几组接口，不要写命令行入口。

### 提交的理由（未改动）

> A 最要紧的问题是解码器到交付时仍不能用。它的主表只有十位宽，遇到十一位符号的前缀没建出子表，而真实日志压出的动态块里字面量码长普遍超过十位。题目要求能正确解析任意合法的 DEFLATE 流，这一缺陷在交付前始终没有修复，也没有一次完整通过的测试记录。README.md 里那张逐档取舍表同样有问题，数字都按实测口径写着，可 benchmarks 下的脚本一次都没执行过，使用方照表选档位就会被误导。
>
> B 的实现过程比 A 曲折，中间多次返工，起初没有查阅 DEFLATE 规范，想靠探针反解 zlib 的产物推码表；此后改用正式解压器作为对照工具，生成各压缩级别的数据流验证，逐项修正了这些缺陷。最终提交的实现是可用的，区间解压、索引项和 gzip 多成员拼接都成立，测试目录中的用例和取舍示例都实际执行过。
>
> A 在跳转边界语义、分块写入的流式入口和 gzip 头兼容上确实比 B 做得好；但解码器在最常见的那档码长上失效，整个库就用不了，综合来看 B 更好。

### 理由　改后

> （没有改后版本。这道题不是某句话写错，错的那句正是判断的立足点，换掉它就等于换结论，只能重跑分析。原因见下。）

### 为什么判断站不住

理由认定 A 的解码器不能用（主表只有十位宽、十一位符号的前缀没建出子表），并据此判 B 更好。但 A 交付的 src/huffman.ts 里 PRIMARY_BITS 之外有完整的两级子表构造，A 自己的收尾自述也写着 huffman.ts 是两级解码表；末轮 tsc 干净、全部用例通过，其中包含 zlib 各压缩级别、多种分布与窗口边界的双向互通。理由里另一句「benchmarks 下的脚本一次都没执行过」同样不成立，A 跑过 tradeoff 与 realistic 两个基准并拿到了体积比与吞吐数字。

错的那句：「这一缺陷在交付前始终没有修复，也没有一次完整通过的测试记录」与「benchmarks 下的脚本一次都没执行过」。

判断落在哪一句：「但解码器在最常见的那档码长上失效，整个库就用不了，综合来看 B 更好」——B 胜出全靠这一条。

删掉错的那句之后剩下什么：同一段理由自己写着 A 在跳转边界语义、分块写入的流式入口和 gzip 头兼容上比 B 做得好，而记在 B 名下的是过程绕远。去掉两句不成立的指控，剩下的材料指向的是 A，结论很可能要翻过来。

---

## 26　平台提交 ID 5920

- 结论：B 更好
- 上传时间：2026-09-22 02:09:34.269157
- 题目类型：Bug 修复　难度：困难　语言：JavaScript, Node.js, Mocha
- 仓库：https://github.com/gy-vs/js-module-import-core
- 处理：正文已订正

### prompt 原文

> 这个仓库是我们构建流水线里用的 JS 压缩器。我们的页面用按需加载，路由对应的模块是在运行时动态引入的，引入路径由一个前缀常量和路由名拼出来。
>
> 我们有一个构建后的检查步骤，扫描产物里所有动态引入的路径字面量，生成一份预加载清单交给 CDN 预热。这个步骤最近开始报空清单。我去看了产物，发现那些路径在压缩之后还是原样的拼接表达式，没有被算成一个字符串。
>
> 我做了几组对照。把同样的字符串拼接写成普通函数调用的参数，压缩之后会被合并成一个字符串常量。写成动态引入的参数就不会，原样留着。用一个只赋值一次的常量拼路径也一样，那个常量在别处会被内联进去，在动态引入的参数里就不会，声明留在原地。看起来动态引入的参数整个被跳过了，不是某一项优化没生效。
>
> 我要的是动态引入的参数和普通调用的参数一样，该做的变换都要做。压缩之后我那份清单能重新生成出来，产物里这些路径是完整的字符串字面量。
>
> 有几点要注意。不要只针对字符串拼接做特判，我要的是这个位置上的表达式能正常参与所有变换，今天是拼接，明天可能是三元表达式选路径。另外，既然这个位置被漏掉了，仓库里其他节点类型有没有同样的遗漏值得一并查一下，如果有就一起处理，不要只补这一处。
>
> 技术上有几点要求。保持仓库现有的技术栈和目录约定，不要引入新的运行时依赖。现有的公开接口和配置项语义不要改动。仓库现有的测试要保持全绿，压缩用例那一套和 mocha 那一套都要过。新增的测试用仓库现有的格式写在现有的压缩测试目录下，要覆盖动态引入参数里的常量折叠和变量内联两类情况。

### 理由　改前（平台上现在是这一版）

> 两边都定位到 lib/transform.js 里 AST_DynamicImport 没注册 descend，补法也一样，差距体现在打开遍历之后带来的连带影响上。
>
> B 发现了两处 A 完全没有处理的问题，关键是 lib/output.js 那处。序列表达式加不加括号的父节点判定只认调用节点，于是动态引入里的逗号表达式被打印成两个实参，而第二个实参是选项位置，实际行为和原代码不一样了。这并不是可有可无的细节，序列合并一类的优化会把序列表达式移入参数位置，而这恰恰是本次打开遍历之后才会出现的情况。另一处是把动态引入补进 reduce-vars 的逃逸边界。
>
> A 的长处是结论可核验，写脚本把节点继承关系和注册表交叉比对，确认只有动态引入缺注册。不足之处在于它只验证了修复本身是否生效，没有继续排查遍历打开后新出现的影响，lib/output.js 那处就是这么漏掉的。B 的问题出在交付环节，修改完之后没有再运行完整回归。漏跑回归带来的只是尚未确认的风险，而打印出行为不一致的代码会直接影响拿到产物的使用者，综合来看 B 更好。

### 理由　改后

> 两边都定位到 lib/transform.js 里 AST_DynamicImport 没注册 descend，补法也一样，差距体现在打开遍历之后带来的连带影响上。
>
> B 发现了两处 A 完全没有处理的问题，关键是 lib/output.js 那处。序列表达式加不加括号的父节点判定只认调用节点，于是动态引入里的逗号表达式被打印成两个实参，而第二个实参是选项位置，实际行为和原代码不一样了。这并不是可有可无的细节，序列合并一类的优化会把序列表达式移入参数位置，而这恰恰是本次打开遍历之后才会出现的情况。另一处是把动态引入补进 reduce-vars 的逃逸边界。
>
> A 的长处是结论可核验，写脚本把节点继承关系和注册表交叉比对，确认只有动态引入缺注册。不足之处在于它只验证了修复本身是否生效，没有继续排查遍历打开后新出现的影响，lib/output.js 那处就是这么漏掉的。B 的问题在于自行扩大了改动面，reduce-vars 的逃逸边界收紧并不是用户点名要的，方向虽然和普通调用一致，却会让动态引入参数附近的一些对象更难内联。这类额外改动还能单独回退，而打印出行为不一致的代码会直接影响拿到产物的使用者，综合来看 B 更好。

### 轨迹依据

- B 最后一次改源码之后跑过完整回归：compress 用例、mocha 全量与 eslint 依次通过，「没有再运行完整回归」不成立；换上的这条不足取自 B 自己的改动范围。

---

## 33　平台提交 ID 5921

- 结论：B 更好
- 上传时间：2026-09-22 02:09:38.904285
- 题目类型：Feature 迭代　难度：困难　语言：JavaScript, Node.js, Mocha
- 仓库：https://github.com/gy-vs/css-calc-type-core
- 处理：正文已订正

### prompt 原文

> 这个仓库是我们样式检查工具底层用的 CSS 语法库。我们的设计系统里到处是计算表达式，边距、栅格、字号都靠它算。上个月线上出了一次事故：有人把一个减法的右操作数写漏了单位，写成了百分之百减去 16，没写 px。这条声明浏览器整个丢掉，那个区块的边距归零，整页布局塌了。灰度的时候没人点到那个页面，全量之后十分钟内涌进来两百多条反馈。
>
> 事后我们发现这个库其实能校验值合不合属性的语法，但对计算表达式它只认了个外壳，里面的加减乘除有没有意义完全不看。上面那条错误的写法在它这里是通过的。我要把里面的检查补上，在构建阶段就拦住。
>
> 规则是这样的。表达式里每个操作数都有一个维度，可能是长度、角度、时间、频率、分辨率、百分比，也可能是不带单位的纯数。加法和减法要求两边的维度相同，百分比在某些位置上可以和它所代表的那个维度相加，这取决于它出现在哪个属性里。乘法要求至少一边是纯数。除法要求除数是纯数。整个表达式最后推出来一个维度，再拿它和这个属性期望的维度比。对不上就报错。
>
> 报错要指到出问题的那个子表达式上，不能只说这条声明里的计算表达式有问题。我们的表达式经常嵌套三四层，只报到声明这一级的话，改的人还得自己一层层找。
>
> 要处理的不只是基本的计算函数。取最小值、取最大值、以及带上下界的钳制函数，这几个的参数之间也要维度一致，它们还会和基本计算函数互相嵌套。嵌套的情况下类型要能一层层往上推。
>
> 百分比是最麻烦的地方。同样一个百分号，在宽度这类属性里代表长度，在某些属性里代表纯数，还有些属性根本不接受百分比。这个区别要按属性来判，不能一刀切。
>
> 除以零要报错。除数是一个无法在这个阶段确定值的表达式时，行为要明确，是报错还是放过要说清理由。
>
> 有一条是硬要求。现在能通过校验的样式，改完之后不能开始报错。我们代码库里有几万条声明，任何一条从通过变成报错，都会让整个工具没法上线。这里面包括自定义属性引用这种在校验阶段拿不到具体值的情况，这类要放过，不能因为推不出维度就判定为错。
>
> 技术上有几点要求。保持仓库现有的技术栈和目录约定，不要引入新的运行时依赖。现有的公开接口语义不要改动。仓库现有的测试要保持全绿。新增的测试写在现有测试目录下，用仓库已经在用的测试框架和用例组织方式。不要改仓库的构建配置和打包脚本。

### 理由　改前（平台上现在是这一版）

> 题目把「不能把原本能通过的声明改成报错」列为硬约束，A 的改动违反了这一条。它新增的 lexer-math-check.js 里，line-height 上百分比减长度、opacity 里数值加百分号，用例核对的都是必须报错，可前者的语法本就是 length-percentage，后者的百分号与数值等价，浏览器都接受。A 把合法写法固定成了错误并用测试锁定下来，今后修改 calc 相关代码的人要先把这几条反向的期望逐条改回来。这套用例在交付前也没有完整运行过。
>
> B 把推导放进 generic.js 的 math 匹配器，另起 lib/lexer/calc.js 做递归下降和维度合并，百分比是否允许交由属性匹配时的探针决定，含百分号的表达式换成一个百分号 token 再交属性语法判定。B 的不足是错误通过模块级槽位传回 Lexer，属于隐式的全局状态。
>
> A 在报错定位粒度上更细，但这点优势建立在反复返工的推断之上，B 还有改动前后的对比运行和全量回归，所以 B 更好。

### 理由　改后

> 题目把「不能把原本能通过的声明改成报错」列为硬约束，A 的改动违反了这一条。它新增的 lexer-math-check.js 里，line-height 上百分比减长度、opacity 里数值加百分号，用例核对的都是必须报错，可前者的语法本就是 length-percentage，后者的百分号与数值等价，浏览器都接受。A 把合法写法固定成了错误并用测试锁定下来，今后修改 calc 相关代码的人要先把这几条反向的期望逐条改回来。
>
> B 把推导放进 generic.js 的 math 匹配器，另起 lib/lexer/calc.js 做递归下降和维度合并，百分比是否允许交由属性匹配时的探针决定，含百分号的表达式换成一个百分号 token 再交属性语法判定。B 的不足是错误通过模块级槽位传回 Lexer，属于隐式的全局状态。
>
> A 在报错定位粒度上更细，但这点优势建立在反复返工的推断之上，而把合法写法写成必须报错的期望是眼下就存在的错误，所以 B 更好。

### 轨迹依据

- A 最后一次改 math-check.js 之后 lint 通过，ESM 与 CJS 两套用例都跑完且全部通过。
- 原句拿「B 还跑了全量回归」跟 A 作对比，而 A 同样跑过，这个对比不成立。

---

## 104-gaoyong　平台提交 ID 5963

- 结论：A 更好
- 上传时间：2026-09-22 02:13:29.884116
- 题目类型：0-1 代码生成　难度：困难　语言：TypeScript, Node.js
- 仓库：https://github.com/gy-vs/framed-stream-core
- 处理：未改动，需重跑分析

### prompt 原文

> 我需要一个二进制分帧库，帧格式是固定 magic、无符号 varint 长度、payload 和 CRC32。编码器返回 Uint8Array，增量解码器接收任意大小的数据块并逐帧产出结果。只交付库和测试，不做文件、网络或命令行适配。
>
> magic、varint、payload 和校验和都可能跨块拆开。长度超过上限、varint 溢出或 CRC 错误时，解码器要报告错误并从后续最近的合法 magic 继续；候选 magic 位于损坏 payload 内时，只有后续长度与完整校验都成立才能恢复，不能误产出帧。输入结束时要区分干净结束和半帧。
>
> 解码过程不能反复拼接全部历史字节，已确认无用的前缀要及时释放。用 TypeScript 和 Node.js 20 实现，不引入 framing 或 CRC 库，CRC32 可以单独实现，并覆盖逐字节输入、多个帧同块、伪 magic、超长长度和截断结束。

### 提交的理由（未改动）

> 两份产物都避免反复拼接历史字节，区别在于为此付出的代价。A 用可增长缓冲加头尾指针圈住未消费字节，确认无用才搬一次；B 用块队列配 KMP 扫描做到了真正零拷贝，在消除内存拷贝上做得更彻底。
>
> 问题在于 B 这套双指针数据面反复出错，长度字段被读成错值，KMP 回退后匹配长度也没清零。B 为此把 src/decoder.ts 重写了几遍，最后退回单一绝对偏移。它记录的最后一次运行仍有用例没过，自重叠那条 B 自己也认定是需要继续排查的真实问题，之后没有通过的记录，解码器实现又不在补丁里，无法从源码确认是否修复。后续接手的人要先让那条用例重新通过，才能确认零拷贝方案还存在多少问题。
>
> A 也有做得不好的地方，test/decoder.test.ts 里位翻转写成空操作表达式，核对条件永远成立，这条用例其实什么都没查；产物里另有一处偏移，窗口整体左移后重扫起点仍使用移位前的坐标。但这两处都能在源码中定位和复核，修正起点坐标即可解决；B 则是一条尚未解决的失败用例，加上一份无法查看的实现。所以 A 更好。

### 理由　改后

> （没有改后版本。这道题不是某句话写错，错的那句正是判断的立足点，换掉它就等于换结论，只能重跑分析。原因见下。）

### 为什么判断站不住

理由认定 B 留着一条没解决的失败用例、之后没有通过的记录，并据此判 A 更好。但 B 末尾类型检查通过，全部用例通过，收尾自述也是完成交付。「解码器实现不在补丁里」这半句成立（源文件当时还未被跟踪，补丁按预算截断了），失败用例那半句不成立。

错的那句：「它记录的最后一次运行仍有用例没过……之后没有通过的记录」。

判断落在哪一句：「B 则是一条尚未解决的失败用例，加上一份无法查看的实现。所以 A 更好」——A 胜出全靠这一条。

删掉错的那句之后剩下什么：B 只剩「实现不在补丁里」，而这是材料截断造成的，不是模型的毛病；A 那两处缺陷（位翻转用例写成空操作、窗口左移后重扫起点用了旧坐标）是实打实的。两边对比因此可能变成 B 更好或者持平。

---

## 112-gaoyong　平台提交 ID 5969

- 结论：A 更好
- 上传时间：2026-09-22 02:13:50.122282
- 题目类型：Bug 修复　难度：困难　语言：TypeScript, Marked
- 仓库：https://github.com/gy-vs/markdown-autolink-entity-core
- 处理：正文已订正

### prompt 原文

> 我解析包含字符引用的自动链接时，href 和显示文本经过了不同的解码与转义顺序。像 &amp;、数字引用和表示引号的引用可能在 tokenizer 中先解码，再在 renderer 中进入属性，导致目标被截断、双重转义或生成错误 HTML。
>
> 请统一自动链接 token 保存的原始目标、显示文本和最终 href。字符引用在文本侧按现有规则显示，在属性侧必须先得到正确 URL 再做 HTML 属性转义；mailto、普通 URL 和扩展 renderer 都要收到稳定且不含共享可变状态的 token。
>
> 请覆盖命名与数字引用、引号和与号、mailto、已有百分号转义以及自定义 renderer。普通行内链接和图片路径不能因修复改变，重复 parse 同一输入必须得到相同 token。

### 理由　改前（平台上现在是这一版）

> 题目明确要求，转义顺序可调整，但行内链接和图片的路径不能随之改变，B 恰恰在这一点上没满足要求。它在渲染器里对链接和图片使用同一套处理逻辑，先取最终 URL 再统一转义，自动链接和行内链接没分开，结果行内链接和图片路径里的裸与号在输出里成了实体写法，地址与源文件不再一致。B 也没留用例核对这两类的实际输出，要修好得先补这条用例。
>
> A 把改动限定在自动链接这条处理路径内，分词阶段该 token 直接取自源串并加上标记，渲染时只对带标记的 token 做属性转义，这一顺序与需求一致；它还加了用例，把裸与号原样输出固定下来。代价是 Tokens.Link 多出一个对外可见的布尔字段。
>
> B 的实现思路有其合理之处，新增 src/entities.ts 承载完整实体表，字面要求上比 A 贴合得更紧。问题是它把显示文本也提前转义，还按内容有条件地追加字段，token 形状随输入变化，最后一批源码改动后也没有成功的构建或测试记录。多出的布尔字段以后还能去掉，而输出中被错误转义的裸与号是眼下就已经存在的错误，所以 A 更好。

### 理由　改后

> 题目明确要求，转义顺序可调整，但行内链接和图片的路径不能随之改变，B 恰恰在这一点上没满足要求。它在渲染器里对链接和图片使用同一套处理逻辑，先取最终 URL 再统一转义，自动链接和行内链接没分开，结果行内链接和图片路径里的裸与号在输出里成了实体写法，地址与源文件不再一致。B 也没留用例核对这两类的实际输出，要修好得先补这条用例。
>
> A 把改动限定在自动链接这条处理路径内，分词阶段该 token 直接取自源串并加上标记，渲染时只对带标记的 token 做属性转义，这一顺序与需求一致；它还加了用例，把裸与号原样输出固定下来。代价是 Tokens.Link 多出一个对外可见的布尔字段。
>
> B 的实现思路有其合理之处，新增 src/entities.ts 承载完整实体表，字面要求上比 A 贴合得更紧。问题是它把显示文本也提前转义，还按内容有条件地追加字段，token 形状随输入变化，拿到这一版的人要先弄清 token 会长成什么样才敢往下改。多出的布尔字段以后还能去掉，而输出中被错误转义的裸与号是眼下就已经存在的错误，所以 A 更好。

### 轨迹依据

- B 最后一次改源码之后反复跑过构建与测试，末轮单元用例、规格用例、lint 与类型检查都通过。

---

## 120-gaoyong　平台提交 ID 5976

- 结论：B 更好
- 上传时间：2026-09-22 02:14:24.658698
- 题目类型：Feature 迭代　难度：困难　语言：Python, attrs
- 仓库：https://github.com/gy-vs/attrs-pickle-state-core
- 处理：正文已订正

### prompt 原文

> 我需要 attrs 类提供 getstate_setstate 开关，控制 slots 类是否自动生成 __getstate__ 和 __setstate__。默认行为保持当前兼容性，显式关闭时必须让用户自定义方法或基类协议接管，显式开启时生成的方法要覆盖 attrs 字段和 weakref 约束。
>
> 请把选项接入 attr.s、attrs.define 及相关类型声明。继承链上父子类选择不同值时，最终类不能重复序列化字段或跳过新增字段；frozen 类恢复状态不能走普通 setattr，带缓存 hash 的类反序列化后要重新计算而不是保存旧缓存。
>
> 请覆盖 slots 与非 slots、frozen、弱引用、缓存 hash、多层继承、用户自定义状态方法以及不同 pickle protocol。生成与否要能从类字典客观判断，不能只让测试碰巧通过。

### 理由　改前（平台上现在是这一版）

> 这道题的关键差别在于，三态开关是复用仓库中已有的机制，还是另外实现一套。B 把判断交给已有的实现判定函数，这几条规则与表示、相等、哈希的处理方式完全一致；设状态使用绑定好的原生赋值，冻结类在恢复状态时不会触发禁止赋值的限制。
>
> A 把三态解析放在构建器的构造函数中完成，再由外部读取构建器的属性来进入另一个分支；哈希缓存的识别方式是检查 __hash__ 的默认参数中是否存在那个包装类型，注释却说自己在看闭包。生成方式一变，或者用户自定义了哈希，重置就会无声跳过，失败也被忽略，冻结类改完字段后哈希还留着旧值，必须重写这段识别逻辑才能修复。A 的 tests/test_getstate_setstate.py 在最后两次改动之后也没有再运行过。
>
> A 有两处做得更好。字段名在运行时从类自身的属性表计算，父类生成的方法被子类继承后仍能覆盖新增字段；保留元组形式的状态，旧版本的数据仍能正常反序列化。B 这两处都有损失，改用字典逐个判断键是否存在，面对旧的元组状态时这些判断几乎都不成立。但 B 的新老用例一起运行过，核对的是实际运行得到的结果。综合来看，B 更好。

### 理由　改后

> 这道题的关键差别在于，三态开关是复用仓库中已有的机制，还是另外实现一套。B 把判断交给已有的实现判定函数，这几条规则与表示、相等、哈希的处理方式完全一致；设状态使用绑定好的原生赋值，冻结类在恢复状态时不会触发禁止赋值的限制。
>
> A 把三态解析放在构建器的构造函数中完成，再由外部读取构建器的属性来进入另一个分支；哈希缓存的识别方式是检查 __hash__ 的默认参数中是否存在那个包装类型，注释却说自己在看闭包。生成方式一变，或者用户自定义了哈希，重置就会无声跳过，失败也被忽略，冻结类改完字段后哈希还留着旧值，必须重写这段识别逻辑才能修复。A 的 docs/glossary.rst 里关于 slots 类自带状态方法会被覆盖的旧说明也没有同步，新开关上线后文档和实际行为不一致。
>
> A 有两处做得更好。字段名在运行时从类自身的属性表计算，父类生成的方法被子类继承后仍能覆盖新增字段；保留元组形式的状态，旧版本的数据仍能正常反序列化。B 这两处都有损失，改用字典逐个判断键是否存在，面对旧的元组状态时这些判断几乎都不成立。但这两处都能在既有机制上补回来，而哈希缓存那段识别逻辑要整段重写，综合来看，B 更好。

### 轨迹依据

- A 最后一次改动之后跑过该测试文件，还连着几个相关模块一起跑；换上的这条不足在 A 侧轨迹里可以核到。
- 原句拿「B 跑过用例」跟 A 作对比，而 A 同样跑过，这个对比不成立。

