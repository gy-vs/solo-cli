# 待录屏题目

## 03
GitHub：https://github.com/gy-vs/css-syntax-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --input-type=module -e 'import { parse, lexer, propertySyntax } from "./lib/index.js"; console.log(propertySyntax.parse("<length># | auto")); const rule = parse(`@property --x { syntax: "<length>"; inherits: false; initial-value: red; }`).children.first; console.log(lexer.checkPropertyRule(rule)); console.log(lexer.checkCustomProperties(parse(`@property --x { syntax: "<length>"; inherits: false; initial-value: 0px; } .a { --x: red; } .b { --x: var(--y); }`)));'`
`npm run lint`
`npm run esm-to-cjs`
3. 跑点名的那几条用例
`npx mocha lib/__tests/property-syntax-parse.js lib/__tests/lexer-check-property-rule.js lib/__tests/lexer-check-custom-properties.js --require lib/__tests/helpers/setup.js --reporter spec`
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --input-type=module -e 'import { parse, lexer } from "./lib/index.js"; import { parsePropertySyntax } from "./lib/lexer/index.js"; console.log(JSON.stringify(parsePropertySyntax("<length> <color> | red <image># | auto"))); const rule = parse(`@property --x { syntax: "<length>"; inherits: false; initial-value: 0; }`).children.first; console.log(lexer.checkPropertyRule(rule).descriptor, lexer.checkPropertyRule(rule).message); console.log(lexer.checkCustomProperties(parse(`@property --x { syntax: "<length>"; inherits: false; initial-value: 0px; } .a { --x: red; } .b { --x: var(--y); }`)));'`
`npm run lint`
3. 跑点名的那几条用例
`npx mocha lib/__tests/lexer-property-syntax.js lib/__tests/lexer-check-property-rule.js lib/__tests/lexer-check-custom-properties.js --require lib/__tests/helpers/setup.js --reporter spec`
`npm run esm-to-cjs; npm run test:cjs`
4. 跑完整测试套件
`npm test`

## 04
GitHub：https://github.com/gy-vs/chrono-datetime-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx babel-node <临时脚本文件>`
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest`
`$env:TZ = 'America/New_York'; npx jest test/duration`
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest`
4. 跑完整测试套件
`npm run test`
B 侧无法给出可运行的启动步骤。

## 07
GitHub：https://github.com/gy-vs/regex-engine-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python --version`
`python -m unittest discover -s tests -v`
`python -c "from posixre import match; m = match(r'(a|ab)(c|bcd)(d*)', 'abcd'); print(m.span(), m.groups(), [m.span(g) for g in (1,2,3)])"`
```powershell
@'
import time; from posixre import compile
p = compile(r'(a|aa)*b')
for n in (20,40,60,80):
    t = 'a'*n; s = time.perf_counter(); r = p.match(t); print(n, r, round((time.perf_counter()-s)*1000, 3), 'ms')
'@ | python -

```
`python -c "from posixre import scan; print([m.span() for m in scan(r'a*', 'bab')])"`
```powershell
@'
from posixre import compile
try:
    compile(r'(x)\\1')
except Exception as e:
    print(type(e).__name__, e, getattr(e, 'pos', None))
'@ | python -

```
3. 跑点名的那几条用例
4. 跑完整测试套件
`python -m pytest -q`

**B** · 认不出形态
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python --version`
`python -m unittest discover -s tests -v`
`python -c "import posixre; m = posixre.match(r'(a|ab)(c|bcd)(d*)', 'abcd'); print(m.span(), m.groups(), [m.span(g) for g in (1,2,3)])"`
```powershell
@'
import time, posixre
p = posixre.compile(r'(a|aa)*b')
for n in (20,40,60,80):
    t = 'a'*n; s = time.perf_counter(); r = p.match(t); print(n, r, round((time.perf_counter()-s)*1000, 3), 'ms')
'@ | python -

```
`python -c "import posixre; print([m.span() for m in posixre.finditer(r'a*', 'aba')])"`
```powershell
@'
import posixre
for pat in (r'a\\1', r'a(?=b)'):
    try:
        posixre.compile(pat)
    except posixre.PatternError as e:
        print(pat, e.pos, e)
'@ | python -

```
3. 跑点名的那几条用例
4. 跑完整测试套件

## 08
GitHub：https://github.com/gy-vs/type-inference-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`npm run typecheck`
`node demo.mjs`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node demo.mjs`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 10
GitHub：https://github.com/gy-vs/css-visitor-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`corepack enable`
`corepack pnpm unit`
`node -r ts-node/register/transpile-only node_modules/uvu/bin.js test "visitor\.test\.ts$"`
3. 跑点名的那几条用例
`corepack pnpm test:lint`
`corepack pnpm test:types`
4. 跑完整测试套件
`pnpm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`corepack enable`
`corepack pnpm unit`
`node -r ts-node/register/transpile-only node_modules/uvu/bin.js test "visitor\.test\.ts$"`
3. 跑点名的那几条用例
`corepack pnpm test:lint`
`corepack pnpm test:types`
4. 跑完整测试套件
`pnpm run test`

## 15
GitHub：https://github.com/gy-vs/query-eval-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -e 'const jsonata=require(''./src/jsonata'');(async()=>{const b={steps:5000,depth:200};try{await jsonata(''$map($, function($x){$map([$x], function($y){$y+1})})'').evaluate(Array.from({length:100000},(_,i)=>i),undefined,b);}catch(e){console.log(e.code,e.budgetExceeded,e.consumed,e.position);}console.log(''used'',b.stepsUsed,''peak'',b.depthPeak);})()'`
`node -e 'const jsonata=require(''./src/jsonata'');(async()=>{const run=async()=>{const b={};await jsonata(''$sum([1..20].($*2))'').evaluate({},undefined,b);return [b.stepsUsed,b.depthPeak];};console.log(await run(),await run());})()'`
3. 跑点名的那几条用例
`npx mocha test/budget-tests.js`
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -e 'const jsonata=require(''./src/jsonata'');(async()=>{const b={steps:5000,depth:200};try{await jsonata(''$map($, function($x){$x*2})'').evaluate(new Array(100000).fill(1),undefined,undefined,b);}catch(e){console.log(e.code,e.limit,e.value,e.position);}console.log(''used'',b.stepsUsed,''peak'',b.depthPeak);})()'`
`node -e 'const jsonata=require(''./src/jsonata'');(async()=>{const b={depth:50};try{await jsonata(''( $inf := function() { $inf() }; $inf())'').evaluate({},undefined,undefined,b);}catch(e){console.log(e.code,e.limit,e.value,e.position);}console.log(''peak'',b.depthPeak);})()'`
3. 跑点名的那几条用例
`npx mocha test/budget.js`
4. 跑完整测试套件
`npm test`

## 16
GitHub：https://github.com/gy-vs/deflate-codec-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`npm run bench 20000000`
`node -e "const s=require('./dist/src/index.js');const d=Buffer.from('log line\n'.repeat(20000));const {data,index}=s.gzipSyncIndexed(d,1<<16);console.log(data.length,index.length,s.gunzipRangeSync(data,index,1000,1100).length)"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`npm run tradeoff 20000000`
`node --input-type=module -e "import {compressSeekable,inflateRange} from './dist/index.js';const d=new TextEncoder().encode('log line\n'.repeat(20000));const r=compressSeekable(d,1<<16);console.log(r.gzip.length,r.index.length,inflateRange(r.deflate,r.index,1000,1100).length);"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 17
GitHub：https://github.com/gy-vs/consensus-replication-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node dist/scripts/smoke.js 1`
`node --max-old-space-size=6144 dist/scripts/debug.js 1 200`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test`

## 24
GitHub：https://github.com/gy-vs/css-custom-prop-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`corepack enable pnpm`
`pnpm i --frozen-lockfile`
`pnpm unit`
`npx uvu -r ts-node/register/transpile-only test stringify.test.ts`
`node -e "const postcss=require('./lib/postcss.js');const css='a{*--x: 1 /* c */}';const out=postcss.parse(css).toString();console.log(JSON.stringify(out), out===css)"`
`npx check-dts`
3. 跑点名的那几条用例
`npx eslint lib/parser.js test/stringify.test.ts`
4. 跑完整测试套件
`pnpm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`pnpm i --frozen-lockfile`
`pnpm unit`
`node ./node_modules/uvu/bin.js -r ts-node/register/transpile-only test stringifier.test`
`node -e "const postcss=require('./lib/postcss.js');const css='a { *--x: red /* note */ }';const root=postcss.parse(css);console.log(root.toString()===css);root.first.first.value='blue';console.log(root.toString())"`
`npx c8 --reporter=text pnpm unit`
3. 跑点名的那几条用例
`pnpm test:lint; pnpm test:types; pnpm test:integration`
4. 跑完整测试套件
`pnpm run test`

## 26
GitHub：https://github.com/gy-vs/js-module-import-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx eslint lib`
`node --input-type=module -e 'import {minify} from "./main.js"; const r = await minify(`function go(){var P="/routes/";return import(P+"home"+".js")}`, {compress:true, mangle:false}); console.log(r.code)'`
3. 跑点名的那几条用例
`node test/compress.js dynamic-import.js`
4. 跑完整测试套件
`node test/compress.js`
`npx mocha test/mocha`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --input-type=module -e 'import {minify} from "./main.js"; const r = await minify(`function go(){var P="/routes/";return import(P+"home"+".js")}`, {compress:true, mangle:false}); console.log(r.code)'`
`node --input-type=module -e 'import {minify} from "./main.js"; const r = await minify(`import((sideEffect(),"/routes/home.js"));`, {compress:true, mangle:false}); console.log(r.code)'`
`npx eslint lib`
3. 跑点名的那几条用例
`node test/compress.js dynamic-import.js`
4. 跑完整测试套件
`node test/compress.js`
`npx mocha test/mocha`

## 33
GitHub：https://github.com/gy-vs/css-calc-type-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --input-type=module -e "import { lexer } from './lib/index.js'; const r = lexer.matchProperty('width', 'calc(100% - 16)'); console.log(r.matched, r.error; r.error.message);"`
`node --input-type=module -e "import { parse, lexer, walk } from './lib/index.js'; const ast = parse('a{ width: calc(1px + calc(2px + 3s)); }', { positions: true }); let d; walk(ast, { visit: 'Declaration', enter(n){ d = n; } }); const r = lexer.matchDeclaration(d); console.log(r.error.rawMessage, r.error.css.substr(r.error.mismatchOffset, r.error.mismatchLength));"`
3. 跑点名的那几条用例
`npx mocha lib/__tests/lexer-math-check.js --require lib/__tests/helpers/setup.js --reporter spec`
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --input-type=module -e "import { lexer } from './lib/index.js'; for (const [p, v] of [['width','calc(100% - 16)'],['width','calc(100% - 16px)'],['border-width','calc(100% - 16px)'],['width','calc(1px + 1s)'],['width','calc(1px / 0)']]) { const r = lexer.matchProperty(p, v); console.log(p, v, r.matched ? 'PASS' : r.error.rawMessage, r.matched ? '' : r.error.mismatchOffset); }"`
`npx eslint lib scripts`
3. 跑点名的那几条用例
`npx mocha lib/__tests/lexer-match-calc.js --require lib/__tests/helpers/setup.js --reporter spec`
`node scripts/esm-to-cjs.cjs; npm run test:cjs`
4. 跑完整测试套件
`npm test`

## 35
GitHub：https://github.com/gy-vs/css-input-map-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node ./node_modules/uvu/bin.js -r ts-node/register/transpile-only test "previous-map\.test\.ts$"`
`corepack pnpm unit`
`git status --porcelain --untracked-files=all`
3. 跑点名的那几条用例
`corepack pnpm test:lint`
`corepack pnpm test:types`
`corepack pnpm test:coverage`
4. 跑完整测试套件
`pnpm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm i -g pnpm`
`pnpm exec uvu -r ts-node/register/transpile-only test "previous-map.test.ts$"`
`pnpm unit`
`git -c safe.directory=. status --porcelain`
3. 跑点名的那几条用例
`pnpm test:lint`
`pnpm test:types`
`pnpm test:coverage`
4. 跑完整测试套件
`pnpm run test`

## 36-gaoyong
GitHub：https://github.com/gy-vs/http-cache-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`npm run typecheck`
`node -e "import('./dist/index.js').then(m => console.log(Object.keys(m).sort().join(',')))"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node --input-type=module -e "import * as k from './dist/index.js'; console.log(Object.keys(k).join(','))"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 37-gaoyong
GitHub：https://github.com/gy-vs/yaml-emit-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`git submodule update --init --recursive`
`npm run build`
`node -e "const YAML=require('./dist/index.js');const A=require('node:assert');const jobs=[{name:'build',image:'app:1.2',env:'prod',retries:3},{name:'test',image:'app:1.2',env:'prod',retries:3},{name:'deploy',image:'app:1.2',env:'prod',retries:3}];const out=YAML.stringify(jobs,{merge:true,mergeCommonEntries:true});console.log(out);A.deepStrictEqual(YAML.parse(out,{merge:true}),jobs)"`
3. 跑点名的那几条用例
`npx vitest run tests/doc/mergeCommonEntries.ts tests/properties.ts`
`npx vitest run`
`npm run test:types; npx eslint src/`
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`git submodule update --init --recursive`
`npm run build`
`node -e "const YAML=require('./dist/index.js');const A=require('node:assert');const jobs=[{name:'build',image:'node:20',retries:3,env:'prod',script:'build'},{name:'test',image:'node:20',retries:3,env:'prod',script:'test'},{name:'deploy',image:'node:20',retries:3,env:'prod',script:'deploy'}];const out=YAML.stringify(jobs,{version:'1.1',mergeCommonKeys:true});console.log(out);A.deepStrictEqual(YAML.parse(out,{version:'1.1'}),jobs)"`
3. 跑点名的那几条用例
`npx vitest run tests/doc/mergeCommonKeys.ts tests/properties.ts`
`npx vitest run`
`npm run test:types; npm run lint`
4. 跑完整测试套件
`npm run test`

## 38-gaoyong
GitHub：https://github.com/gy-vs/yaml-alias-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx tsc --noEmit`
`npx tsc --noEmit -p tests/tsconfig.json`
`npm run lint`
`npm run build`
3. 跑点名的那几条用例
`npx vitest run tests/doc/parse.ts tests/doc/anchors.ts`
`npx vitest run`
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx tsc --noEmit -p tsconfig.json`
`npx eslint src/ tests/doc/anchors.ts tests/doc/parse.ts`
3. 跑点名的那几条用例
`npx vitest run tests/doc/parse.ts tests/doc/anchors.ts`
`npx vitest run --exclude tests/json-test-suite.ts --exclude tests/yaml-test-suite.ts`
4. 跑完整测试套件
`npm run test`

## 39-gaoyong
GitHub：https://github.com/gy-vs/chrono-recur-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node -e "const {RecurrenceRule, DateTime, Interval} = require('./build/node/luxon.js'); const start = DateTime.fromObject({year:2026,month:5,day:1,hour:9},{zone:'America/New_York'}); const r = RecurrenceRule.fromString('FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=-1', start); console.log(r.take(3).map(d=>d.toISO())); console.log(r.between(Interval.fromDateTimes(DateTime.fromObject({year:2026,month:5,day:1},{zone:'America/New_York'}), DateTime.fromObject({year:2026,month:7,day:1},{zone:'America/New_York'}))).map(d=>d.toISO()));"`
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest test/recurrenceRule.test.js`
`$env:TZ = 'America/New_York'; npx jest`
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build-node`
`node -e "const {Recurrence, DateTime} = require('./build/node/luxon.js'); const start = DateTime.fromObject({year:2026,month:5,day:1,hour:9},{zone:'America/New_York'}); const r = Recurrence.fromRRule('FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=-1', start); console.log(r.take(3).map(d=>d.toISO())); console.log(r.between(DateTime.fromObject({year:2026,month:5,day:1},{zone:'America/New_York'}), DateTime.fromObject({year:2026,month:7,day:1},{zone:'America/New_York'})).map(d=>d.toISO()));"`
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest test/recurrence`
`$env:TZ = 'America/New_York'; npx jest`
4. 跑完整测试套件
`npm run test`

## 40-gaoyong
GitHub：https://github.com/gy-vs/chrono-format-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest`
`$env:TZ = 'America/New_York'; npx jest test/impl/zoneNameCache.test.js`
4. 跑完整测试套件
`npx prettier --check 'src/**/*.js' 'test/**/*.js'`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run benchmark`
3. 跑点名的那几条用例
`$env:TZ = 'America/New_York'; npx jest`
`$env:TZ = 'America/New_York'; npx jest test/impl/zoneInfoCache.test.js`
`npx prettier --check src/impl/util.js src/settings.js test/impl/zoneInfoCache.test.js benchmarks/datetime.js`
4. 跑完整测试套件
`npm run test`

## 41-gaoyong
GitHub：https://github.com/gy-vs/js-using-decl-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:main`
`npm run build:using`
`node -e "const acorn=require('./acorn');const P=acorn.Parser.extend(require('./acorn-using'));console.log(JSON.stringify(P.parse('using handle = open(path)',{ecmaVersion:2022}).body[0].kind));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:using`
`node -e "const acorn=require('./acorn');const P=acorn.Parser.extend(require('./acorn-using'));console.log(P.parse('using handle = open(path)',{ecmaVersion:2022}).body[0].kind);console.log(P.parse('using\nx = 1',{ecmaVersion:2022}).body.length);"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`
`node test/run.js`

## 42-gaoyong
GitHub：https://github.com/gy-vs/js-token-context-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node -e "const acorn=require('./acorn/dist/acorn.js');for(const c of ['foo.if() / 2','foo.while() / 2','foo.for() / 2','foo.with() / 2','foo.iff() / 2','foo.class() / 2']){console.log(c, acorn.parse(c,{ecmaVersion:2020}).body.length)}"`
`node -e "const acorn=require('./acorn/dist/acorn.js');const a=acorn.parse('foo.if()\n/a/g.test(x)',{ecmaVersion:2020});console.log(a.body.length, JSON.stringify(a.body[0].expression.type))"`
`node -e "const loose=require('./acorn-loose/dist/acorn-loose.js');console.log(loose.parse('foo.if()\n/a/g.test(x)',{ecmaVersion:2020}).body.length)"`
`npm run lint`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node test/run.js`
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node -e "const acorn=require('./acorn/dist/acorn.js');for(const c of ['foo.if() / 2','foo.while() / 2','foo.for() / 2','foo.with() / 2','foo.iff() / 2','foo.class() / 2']){console.log(c, acorn.parse(c,{ecmaVersion:2020}).body.length)}"`
`node -e "const acorn=require('./acorn/dist/acorn.js');const a=acorn.parse('foo.if()\n/a/g.test(x)',{ecmaVersion:2020});console.log(a.body.length, a.body[0].expression.operator)"`
`node -e "const acorn=require('./acorn/dist/acorn.js');console.log([...acorn.tokenizer('foo.if() / 2',{ecmaVersion:2020})].map(t=>t.type.label).join(' '))"`
`node -e "const acorn=require('./acorn/dist/acorn.js');for(const c of ['foo?.if() / 2','x.function * 2']){console.log(c, acorn.parse(c,{ecmaVersion:2020}).body[0].expression.operator)}"`
`npm run lint`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node test/run.js`
`npm test`

## 43-gaoyong
GitHub：https://github.com/gy-vs/css-generator-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -e "import('./lib/index.js').then(({parse,generate})=>{console.log(generate(parse('span#foo{color:red}',{parseRulePrelude:false})));console.log(generate(parse('x{--a:1%var(--b)#ff0000}')));console.log(generate(parse('li.item{color:red}',{parseRulePrelude:false})));console.log(generate(parse('@media screen{a{color:red}}',{parseAtrulePrelude:false})));})"`
`npm run lint`
`npm run build`
3. 跑点名的那几条用例
`npx mocha lib/__tests --require lib/__tests/helpers/setup.js --reporter dot`
`npx mocha cjs/__tests --require lib/__tests/helpers/setup.js --reporter dot`
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -e "import('./lib/index.js').then(({parse,generate})=>{console.log(generate(parse('span#foo{color:red}',{parseRulePrelude:false})));console.log(generate(parse('li.item{color:red}',{parseRulePrelude:false})));console.log(generate(parse('x{--a:1%var(--b)#ff0000}')));console.log(generate(parse('a{margin:1px 2px;border:1px solid #ff0000}')));})"`
`npx eslint lib scripts`
`npm run build`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`
`npm run test:cjs`
`npm run test:dist`

## 45-gaoyong
GitHub：https://github.com/gy-vs/query-signature-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`.\node_modules\.bin\eslint.cmd src`
`.\node_modules\.bin\nyc.cmd check-coverage --statements 100 --branches 100 --functions 100 --lines 100`
`node -e 'const jsonata=require(''./src/jsonata.js'');jsonata(''λ($arg1,$arg2)<n+>{[$arg1,$arg2]}(1,2)'').evaluate({}).then(r=>console.log(JSON.stringify(r)))'`
3. 跑点名的那几条用例
4. 跑完整测试套件
`.\node_modules\.bin\mocha.cmd "test/**/*.js"`
`.\node_modules\.bin\nyc.cmd --reporter=text .\node_modules\.bin\_mocha.cmd -- "test/**/*.js"`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`.\node_modules\.bin\eslint.cmd src`
`node -e 'const jsonata=require(''./src/jsonata'');jsonata(''λ($arg1,$arg2)<n+>{[$arg1,$arg2]}(1,2)'').evaluate({}).then(r=>console.log(JSON.stringify(r)))'`
`node -e 'const jsonata=require(''./src/jsonata'');jsonata(''λ($a,$b)<a<n>+:o>{[$a[0],$b[0]]}([1,9],[2,8],[3])'').evaluate({}).then(r=>console.log(JSON.stringify(r)))'`
3. 跑点名的那几条用例
`.\node_modules\.bin\mocha.cmd test/run-test-suite.js --grep "function-signatures"`
4. 跑完整测试套件
`.\node_modules\.bin\mocha.cmd -- "test/**/*.js"`

## 51-gaoyong
GitHub：https://github.com/gy-vs/dns-resolution-cache

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node --input-type=module -e "import { DnsCache, ManualClock } from './dist/index.js'; const clock = new ManualClock(0); const cache = new DnsCache({ resolver: { async resolve(host, type) { return { kind: 'answer', records: ['1.2.3.4'], ttlMs: 60000 }; } }, clock, capacity: 8, staleWindowMs: 30000 }); console.log(await cache.lookup('Example.COM.', 'a')); console.log(cache.inspect('example.com', 'A')); cache.close();"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run typecheck`
`npm run build`
`node --input-type=module -e "import { DnsCache } from './dist/index.js'; const cache = new DnsCache({ resolver: { async resolve(name, type) { return { status: 'ok', answers: [{ type: 'A', name, data: '1.2.3.4', ttlSeconds: 60 }], ttlSeconds: 60 }; } }, clock: { now: () => 0 }, capacity: 8 }); console.log(await cache.resolve('Example.COM.', 'a')); console.log(cache.snapshot()); await cache.close();"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 52-gaoyong
GitHub：https://github.com/gy-vs/range-lock-manager

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`npm run typecheck`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`npm run typecheck`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 53-gaoyong
GitHub：https://github.com/gy-vs/cookie-policy-jar

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run build`
`npm run typecheck`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run build`
`npm run typecheck`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 54-gaoyong
GitHub：https://github.com/gy-vs/hedged-request-coordinator

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
3. 跑点名的那几条用例
`node --test --test-name-pattern="external cancellation during backoff" dist/test/coordinator.test.js`
4. 跑完整测试套件
`npm test`
`node --test dist/test/coordinator.test.js`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node -e "import('./dist/src/index.js').then(async ({ RequestCoordinator, VirtualClock, RetryableError }) => { const clock = new VirtualClock(); const c = new RequestCoordinator({ clock, sleeper: clock, random: () => 0.5, hedgeDelayMs: 100, budgetTokens: 4 }); const p = c.execute({ deadlineMs: clock.now() + 1000, idempotent: true, attempt: async () => 'ok' }); await clock.runAll(); console.log(await p); })"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`
`node --test dist/test/`

## 55-gaoyong
GitHub：https://github.com/gy-vs/accept-negotiation-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node --input-type=module -e "import { negotiate, varyFor } from './dist/src/index.js'; const variants = [{ id: 'a', mediaType: 'text/html;level=1', language: 'en-US', encoding: 'gzip', charset: 'utf-8' }, { id: 'b', mediaType: 'application/json', language: 'en' }]; const r = negotiate(variants, { headers: { accept: 'text/html;q=0.9, text/*;q=0.5, */*;q=0.1', acceptLanguage: 'en-US, en;q=0.8', acceptEncoding: 'gzip, br;q=0.8' }, weights: { mediaType: 3, language: 2, encoding: 1, charset: 1 } }); console.log(JSON.stringify(r, null, 2)); console.log(varyFor({ accept: 'text/html', acceptLanguage: 'en' }));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node --input-type=module -e "import { negotiate, computeVary } from './build/src/index.js'; const candidates = [{ mediaType: 'text/html;level=1', language: 'en-US', data: { id: 1 } }, { mediaType: 'application/json', encoding: 'gzip', data: { id: 2 } }]; const request = { accept: 'text/*;q=0.8, application/json', acceptLanguage: 'en;q=0.9', acceptEncoding: 'gzip, identity;q=0' }; const r = negotiate(candidates, request, { weights: { media: 2, language: 1, encoding: 1, charset: 1 } }); console.log(JSON.stringify(r, null, 2)); console.log(computeVary(candidates, request));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 56-gaoyong
GitHub：https://github.com/gy-vs/httpx-tls-client-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 57-gaoyong
GitHub：https://github.com/gy-vs/httpx-zstd-response-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 58-gaoyong
GitHub：https://github.com/gy-vs/httpx-timeout-dispatch-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 59-gaoyong
GitHub：https://github.com/gy-vs/httpx-digest-auth-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 60-gaoyong
GitHub：https://github.com/gy-vs/httpx-asgi-transport-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 61-gaoyong
GitHub：https://github.com/gy-vs/jinja-bytecode-cache-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_bytecode_cache.py -v`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_bytecode_cache.py -v`
`python -m pytest -q --doctest-modules src/jinja2/bccache.py`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 62-gaoyong
GitHub：https://github.com/gy-vs/jinja-async-stream-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_async.py tests/test_async_filters.py -q`
`python -m pytest tests/test_async.py -q -k "generate_async or async_extend"`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_async.py tests/test_async_filters.py -q`
`python -m pytest tests/test_async.py -q -k "generate_async or short_circuit or exactly_once or render_error"`
`python -m pytest tests/test_async_filters.py -q -k "short_circuit or does_not_mask"`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

## 63-gaoyong
GitHub：https://github.com/gy-vs/jinja-branch-scope-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python -c "from jinja2 import Environment; t=Environment().from_string('{% if x %}{{ a.b }}{% set a = 1 %}{% elif y %}{% set a = 2 %}{% else %}{% set a = 3 %}{% endif %}{{ a }}'); print(t.render(x=True, a=type('O',(),{'b':'value'})())); print(t.render(y=True)); print(t.render())"`
`python -c "from jinja2 import Environment; print(Environment().compile('{% if x %}{{ a }}{% set a = 1 %}{% elif y %}{% set a = 2 %}{% else %}{% set a = 3 %}{% endif %}{{ a }}', raw=True))"`
3. 跑点名的那几条用例
`python -m pytest tests/test_regression.py tests/test_compile.py tests/test_idtracking.py -q`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`$env:PYTHONPATH = 'src'; python -c "import jinja2; t=jinja2.Environment().from_string('{% if cond %}{{ a.b }}{% set a = 1 %}{% elif other %}{% set a = 2 %}{% else %}{% set a = 3 %}{% endif %}{{ a }}'); print(t.render(cond=True, a=type('O',(),{'b':'value'})()))"`
`$env:PYTHONPATH = 'src'; python -c "import jinja2; print(jinja2.Environment().compile('{% if cond %}{{ a.b }}{% set a = 1 %}{% elif other %}{% set a = 2 %}{% else %}{% set a = 3 %}{% endif %}', raw=True))"`
3. 跑点名的那几条用例
`$env:PYTHONPATH = 'src'; python -m pytest tests/ -q`
`$env:PYTHONPATH = 'src'; python -m pytest tests/test_idtracking.py tests/test_compile.py tests/test_core_tags.py -q`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 64-gaoyong
GitHub：https://github.com/gy-vs/jinja-required-block-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
```powershell
@'
from jinja2 import Environment, DictLoader; e=Environment(loader=DictLoader({'base':'{% block x required %}
{# c #}
{% endblock %}','child':'{% extends "base" %}{% block x %}ok{% endblock %}'})); print(e.get_template('child').render())
'@ | python -

```
```powershell
@'
from jinja2 import Environment, DictLoader, TemplateSyntaxError; e=Environment(loader=DictLoader({'bad':'{% block x required %}

{% if 1 %}a{% endif %}
{% endblock %}'}));
import sys
try:
    e.get_template('bad')
except TemplateSyntaxError as err:
    print(err.message, err.lineno)
'@ | python -

```
3. 跑点名的那几条用例
`python -m pytest tests/test_inheritance.py -q -k required`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
```powershell
@'
from jinja2 import Environment, DictLoader; e=Environment(loader=DictLoader({'default':'{% block x required %}
 {# a #} 
{% endblock %}','level1':'{% extends "default" %}','level2':'{% extends "level1" %}{% block x %}CHILD{% endblock %}','level3':'{% extends "level2" %}'})); print(e.get_template('level3').render())
'@ | python -

```
```powershell
@'
from jinja2 import Environment, DictLoader, TemplateSyntaxError; e=Environment(loader=DictLoader({'bad':'{% block x required %}
{% block y %}{% endblock %}{% endblock %}'}));
try:
    e.get_template('bad')
except TemplateSyntaxError as err:
    print(err.message, err.lineno)
'@ | python -

```
3. 跑点名的那几条用例
`python -m pytest tests/test_inheritance.py -q -k required`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

## 65-gaoyong
GitHub：https://github.com/gy-vs/attrs-field-transform-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
```powershell
$env:PYTHONPATH = 'src'; @'
import attr
from attr import AliasType

def hook(cls, fields):
    for f in fields:
        print("in transformer:", f.name, f.alias, f.alias_type)
    return [f.evolve(name="_renamed") if f.name == "_private" else f for f in fields]

@attr.s(field_transformer=hook)
class C:
    _private = attr.ib()
    _explicit = attr.ib(alias="kept")

print([(f.name, f.alias, f.alias_type) for f in attr.fields(C)])
print(C(renamed=1, kept=2))
'@ | python -

```
3. 跑点名的那几条用例
`$env:PYTHONPATH = 'src'; python -m pytest tests/test_make.py tests/test_hooks.py -q`
`$env:PYTHONPATH = 'src'; python -m pytest tests/ -q --deselect tests/test_converters.py::TestPipe::test_wrapped_annotation`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 66-gaoyong
GitHub：https://github.com/gy-vs/attrs-keyword-init-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python -c "import attrs; print(attrs.set_kw_only_override, attrs.get_kw_only_override())"`
`python demo_kw_only.py`
3. 跑点名的那几条用例
`python -m pytest tests/test_make.py -k "kw_only or keyword_only" -q`
`python -m pytest tests/test_config.py -k KwOnlyOverride -q`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python -c "import attr; print(attr.set_force_kw_only_override, attr.get_force_kw_only_override())"`
3. 跑点名的那几条用例
`python -m pytest tests/test_kw_only.py -q`
`python -m pytest tests/test_config.py -k force_kw_only -q`
`python -m pytest tests/test_make.py -k "kw_only or keyword_only" -q`
4. 跑完整测试套件
`python -m pytest tests/ -q`
`deactivate`

## 67-gaoyong
GitHub：https://github.com/gy-vs/attrs-slotted-cache-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`curl.exe -sSf https://bootstrap.pypa.io/get-pip.py -o get-pip.py; python get-pip.py --user --break-system-packages`
```powershell
$env:PYTHONPATH = 'src'; @'
import functools, attr

@attr.s(slots=True)
class C:
    @property
    def f(self):
        raise AttributeError("I am a property")

    @functools.cached_property
    def g(self):
        return self.f

try:
    C().g
except AttributeError as e:
    print("propagated:", e)
'@ | python -

```
3. 跑点名的那几条用例
`$env:PYTHONPATH = 'src'; python -m pytest tests/test_slots.py -q`
`$env:PYTHONPATH = 'src'; python -m pytest tests/ -q --ignore=tests/test_pyright.py --ignore=tests/test_packaging.py`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`curl.exe -sS https://bootstrap.pypa.io/get-pip.py -o get-pip.py; python get-pip.py --user --break-system-packages`
```powershell
$env:PYTHONPATH = 'src'; @'
import functools, attr, attrs

@attr.s(slots=True)
class C:
    @property
    def f(self):
        raise AttributeError("I am a property")

    @functools.cached_property
    def g(self):
        return self.f

try:
    C().g
except AttributeError as e:
    print("propagated:", e)

calls = 0

@attrs.frozen(slots=True)
class A:
    x: int

    @functools.cached_property
    def f(self) -> int:
        global calls
        calls += 1
        return self.x * 2

a = A(21)
print(a.f, a.f, calls, hasattr(a, "__dict__"))
'@ | python -

```
3. 跑点名的那几条用例
`$env:PYTHONPATH = 'src'; python -m pytest tests/test_slots.py -q -p no:cacheprovider`
`$env:PYTHONPATH = 'src'; python -m pytest tests/ -q -p no:cacheprovider --ignore=tests/test_packaging.py`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 68-gaoyong
GitHub：https://github.com/gy-vs/attrs-preinit-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_make.py -q -k pre_init`
`python -m pytest tests -q`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
3. 跑点名的那几条用例
`python -m pytest tests/test_make.py -q -k pre_init`
`python -m pytest tests -q --deselect tests/test_packaging.py::TestLegacyMetadataHack`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 69-gaoyong
GitHub：https://github.com/gy-vs/async-queue-window-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --import=tsx/esm --test test/rate-limit.ts`
`node --import=tsx/esm --test test/*.ts`
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
3. 跑点名的那几条用例
`node --import=tsx/esm --test --test-name-pattern="fixed window resets|re-enqueuing before|active handler|carryoverIntervalCount carries" test/advanced.ts`
4. 跑完整测试套件
`node --import=tsx/esm --test test/advanced.ts`
`node --import=tsx/esm --test test/*.ts`
`npm test`

## 70-gaoyong
GitHub：https://github.com/gy-vs/async-queue-backpressure-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx tsc --noEmit`
`git show HEAD:source/index.ts > "$env:TEMP\index-baseline.ts"`
3. 跑点名的那几条用例
`npx xo source/index.ts test/basic.ts`
4. 跑完整测试套件
`node --import=tsx/esm --test --test-name-pattern="onSizeLessThan" test/basic.ts`
`node --import=tsx/esm --test test/*.ts`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx tsc --noEmit`
`npx del-cli dist; npx tsc; npx tsd`
`npx xo`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --import=tsx/esm --test test/basic.ts`
`node --import=tsx/esm --test test/*.ts`

## 71-gaoyong
GitHub：https://github.com/gy-vs/async-queue-abort-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx xo`
`npx del-cli dist; npx tsc; npx tsd`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --import=tsx/esm --test test/abort.ts`
`node --import=tsx/esm --test test/*.ts`
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx xo`
`npx del-cli dist; npx tsc; npx tsd`
3. 跑点名的那几条用例
`1..3 | ForEach-Object { node --import=tsx/esm --test test/*.ts }`
4. 跑完整测试套件
`node --import=tsx/esm --test --test-name-pattern="abort" test/advanced.ts`
`node --import=tsx/esm --test test/*.ts`
`npm test`

## 72-gaoyong
GitHub：https://github.com/gy-vs/async-queue-drain-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx ava`
`npx ava --match "mass abort*"`
`npx tsc --noEmit`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx ava --match "*aborted*"`
`npx tsc`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 73-gaoyong
GitHub：https://github.com/gy-vs/async-queue-interval-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx ava`
`npx ava --match '*intervalCap is strictly enforced*'`
`npx ava --match '*re-entrant*'`
`npx ava --match '*no lingering timers*'`
`git stash -- source/; npx ava --match '*re-entrant*' ; git stash pop`
`npx xo`
`npx tsc --noEmit`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx ava --match '*high concurrency intervalCap*'`
`npx ava --match '*reentrant*'`
`npx ava --match '*hanging timer*'`
`npx xo`
`npx tsc --noEmit`
`npm run build`
3. 跑点名的那几条用例
`npx ava test/test.ts`
`node --import tsx/esm test/fixtures/timer-cleanup.ts`
4. 跑完整测试套件
`npm test`

## 74-gaoyong
GitHub：https://github.com/gy-vs/markdown-reference-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:esbuild`
`npx eslint .`
`npx tsc`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --test --test-reporter=spec test/run-spec-tests.js`
`node --test --test-reporter=spec test/unit/*.test.js`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test:specs`
`npm run test:unit`
`npm test`

## 76-gaoyong
GitHub：https://github.com/gy-vs/markdown-task-list-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:esbuild`
`node -e "import('./lib/marked.esm.js').then(async ({ marked }) => { const fs = await import('node:fs'); console.log(marked.parse(fs.readFileSync('test/specs/new/tasklist_loose_nested.md', 'utf8'))); })"`
`npx tsc --noEmit`
3. 跑点名的那几条用例
`npx eslint src/Tokenizer.ts test/unit/marked.test.js`
4. 跑完整测试套件
`node --test --test-reporter=spec test/run-spec-tests.js`
`node --test --test-reporter=spec test/unit/*.test.js`
`node test/umd-test.js`
`node test/cjs-test.cjs`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:esbuild`
`node -e "import('./lib/marked.esm.js').then(async ({ marked }) => { const fs = await import('node:fs'); console.log(marked.parse(fs.readFileSync('test/specs/new/tasklist_nested_loose.md', 'utf8'))); })"`
3. 跑点名的那几条用例
`node --test --test-reporter=spec --test-name-pattern="GFM task lists" test/unit/marked.test.js`
`npx eslint src/Tokenizer.ts test/unit/marked.test.js`
4. 跑完整测试套件
`node --test --test-reporter=spec test/run-spec-tests.js`
`node --test --test-reporter=spec test/unit/*.test.js`

## 77-gaoyong
GitHub：https://github.com/gy-vs/markdown-blockquote-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:esbuild`
`node -e "import('./lib/marked.esm.js').then(({marked})=>console.log(marked.parse('> > foo\nbar\n> > baz')))"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test:specs`
`node --test test/unit/Lexer.test.js`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx tsx -e "import { marked } from './src/marked.ts'; console.log(marked.parse('> > foo\nbar\n> > baz'))"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm run test:specs`
`npm run test:unit`
`npm run test:cjs`
`npm run test:umd`
`npm run test:lint`
`npm run test:types`

## 78-gaoyong
GitHub：https://github.com/gy-vs/markdown-inline-link-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build:esbuild`
`node -e "import('./lib/marked.esm.js').then(({marked})=>{for(const n of [1000,10000,50000,100000]){const src='[](\"'+' '.repeat(n);const t=Date.now();marked.parse(src);console.log(n, Date.now()-t+'ms');}})"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --test --test-reporter=spec test/run-spec-tests.js`
`node --test test/unit/*.test.js`
`node test/cjs-test.cjs`
`node test/umd-test.js`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node esbuild.config.js`
`node -e "import('./lib/marked.esm.js').then(({marked})=>{for(const n of [1000,10000,50000]){const t=Date.now();marked.parse('[]('+' '.repeat(n)+'\u0000)');console.log(n, Date.now()-t+'ms');}})"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --test --test-reporter=spec test/run-spec-tests.js`
`node --test test/unit/redos.test.js`

## 79-gaoyong
GitHub：https://github.com/gy-vs/httpx-socket-options-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 80-gaoyong
GitHub：https://github.com/gy-vs/httpx-socks-resolution-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 81-gaoyong
GitHub：https://github.com/gy-vs/httpx-zstd-codec-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 82-gaoyong
GitHub：https://github.com/gy-vs/httpx-multipart-header-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 83-gaoyong
GitHub：https://github.com/gy-vs/httpx-h2-tunnel-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 100-gaoyong
GitHub：https://github.com/gy-vs/markdown-token-pipeline-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx rollup -c rollup.config.js`
`npm run build:types`
`node --input-type=module -e "import { marked } from './lib/marked.esm.js'; marked.use({ hooks: { processAllTokens(tokens) { return tokens.filter(t => t.type !== 'hr'); } } }); console.log(marked.parse('paragraph one\n\n---\n\nparagraph two'));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --test --test-reporter=spec test/unit/Hooks.test.js`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npm run build`
`node --input-type=module -e "import { marked } from './lib/marked.esm.js'; marked.use({ hooks: { processAllTokens(tokens) { tokens = tokens.filter(t => t.type !== 'hr'); tokens.unshift({ type: 'heading', raw: '# Added heading\n', depth: 1, text: 'Added heading', tokens: [{ type: 'text', raw: 'Added heading', text: 'Added heading' }] }); return tokens; } } }); console.log(marked.parse('*text*\n\n---\n\nmore text'));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --test --test-reporter=spec test/unit`
`npm run test:specs`

## 102-gaoyong
GitHub：https://github.com/gy-vs/rendezvous-hash-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run build`
`node --input-type=module -e "import { RendezvousRouter } from './dist/index.js'; const r = new RendezvousRouter([{id:'node-a',weight:1},{id:'node-b',weight:4,labels:['rack-2']},{id:'node-c',weight:2,labels:['rack-3']}]); console.log(r.select('user:123'), r.selectReplicas('user:123',2), r.selectBatch(['k1','k2']));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run build`
`node --input-type=module -e "import { RendezvousRouter } from './dist/index.js'; const r = new RendezvousRouter().addNodes([{id:'a',weight:1,domain:'rack-1'},{id:'b',weight:4,domain:'rack-1'},{id:'c',weight:2,domain:'rack-2'}]); console.log(r.pick('user:123'), r.pickReplicas('user:123',2), r.pickMany(['k1','k2']));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 103-gaoyong
GitHub：https://github.com/gy-vs/async-phaser-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node -e "import('./dist/src/phaser.js').then(({ Phaser }) => { const p = new Phaser(); const a = p.register(); const b = p.register(); a.arrive(); console.log(p.snapshot); b.arrive(); console.log(p.snapshot); });"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node -e "import('./dist/src/index.js').then(({ Phaser, PhaserTerminatedError }) => { const p = new Phaser(); const a = p.register(); const b = p.register(); a.arrive(); console.log(p.snapshot); b.arrive(); console.log(p.snapshot); a.arriveAndDeregister(); b.arriveAndDeregister(); console.log(p.snapshot); try { p.register(); } catch (e) { console.log(e.name); } });"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 104-gaoyong
GitHub：https://github.com/gy-vs/framed-stream-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run typecheck`
`npm run build`
`node --input-type=module -e "import { encodeFrame, FrameDecoder } from './dist/index.js'; const d = new FrameDecoder({ onFrame: p => console.log('frame:', new TextDecoder().decode(p)), onError: e => console.log('error:', e.code) }); const f = encodeFrame(new TextEncoder().encode('hello')); for (const b of f) d.push(Uint8Array.of(b)); console.log(d.finish());"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node -v`
`npm run typecheck`
`npm run build`
`node --input-type=module -e "import { FrameEncoder, FrameDecoder } from './dist/index.js'; const wire = new FrameEncoder().encode(new TextEncoder().encode('hello')); const d = new FrameDecoder({ onFrame: ({ payload }) => console.log('frame:', new TextDecoder().decode(payload)), onError: e => console.log('error:', e.code) }); d.write(wire.subarray(0, 3)); d.write(wire.subarray(3)); console.log(d.end());"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 105-gaoyong
GitHub：https://github.com/gy-vs/text-anchor-transform-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node --input-type=module -e "import { transform } from './dist/src/index.js'; const r = transform('hello world', [{ start: 6, end: 8, text: 'XYZ' }], [{ offset: 6, affinity: 'left' }, { offset: 6, affinity: 'right' }]); console.log(r.text, r.anchors, r.reverse.mapBack(7));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`node --version`
`npm run build`
`node --input-type=module -e "import { transform } from './dist/src/index.js'; const r = transform('hello world', [{ start: 5, end: 5, text: '!' }, { start: 6, end: 11, text: 'there' }], [{ kind: 'point', offset: 5, affinity: 'left' }, { kind: 'point', offset: 5, affinity: 'right' }, { kind: 'range', start: 6, end: 11, startAffinity: 'left', endAffinity: 'right' }]); console.log(r.text, r.anchors, r.reverse.query(7), r.reverse.queryMany([0, 5, 7]));"`
3. 跑点名的那几条用例
4. 跑完整测试套件
`npm test`

## 106-gaoyong
GitHub：https://github.com/gy-vs/httpx-url-encoding-core

**A** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

**B** · 纯后端
起服务：`uvicorn main:app --reload`
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 把服务起起来
`uvicorn main:app --reload`
3. 打改动涉及的接口，把响应摆出来
`curl.exe -s http://localhost:<端口>/<接口路径> | python -m json.tool`
4. 打一次异常入参
`curl.exe -s -i http://localhost:<端口>/<接口路径> -d '<不合法的入参>'`
5. 跑测试套件
`python -m pytest -q`
`deactivate`

## 107-gaoyong
GitHub：https://github.com/gy-vs/jinja-template-globals-core

**A** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python -m ensurepip --default-pip`
`python -c "from jinja2 import Environment; from jinja2.loaders import DictLoader; env=Environment(loader=DictLoader({'foo':'{{ bar }}'})); t=env.get_template('foo', globals={'bar':1}); print(t.render()); print(env.get_template('foo', globals={'bar':2}).render()); print('bar' in env.globals)"`
3. 跑点名的那几条用例
`python -m pytest tests/test_loader.py -q -k "globals"`
`python -m pytest tests -q`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

**B** · 无界面（库 / CLI）
`python -m venv .venv`
`.\.venv\Scripts\Activate.ps1`
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`python -m ensurepip --default-pip`
`$env:PYTHONPATH = 'src'; python -c "from jinja2 import Environment; from jinja2.loaders import DictLoader; env=Environment(loader=DictLoader({'foo':'{{ value }}'})); env.globals['value']='env'; t=env.get_template('foo', globals={'value':'one'}); print(t.render()); print(env.get_template('foo', globals={'value':'two'}).render()); print(env.globals['value'])"`
3. 跑点名的那几条用例
`$env:PYTHONPATH = 'src'; python -m pytest tests/test_loader.py tests/test_regression.py -q -k "globals"`
`$env:PYTHONPATH = 'src'; python -m pytest tests -q`
4. 跑完整测试套件
`python -m pytest -q`
`deactivate`

## 110-gaoyong
GitHub：https://github.com/gy-vs/async-queue-signal-cleanup-core

**A** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx xo`
`npx del-cli dist; npx tsc; npx tsd`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --import=tsx/esm --test test/*.ts`
`node --import=tsx/esm --test test/abort-cleanup.ts`

**B** · 无界面（库 / CLI）
1. 先亮出改动范围
`git diff --stat main...HEAD`
2. 跑改动针对的那个场景
`npx xo`
`npx del-cli dist; npx tsc; npx tsd`
3. 跑点名的那几条用例
4. 跑完整测试套件
`node --import=tsx/esm --test test/abort-cleanup.ts`
`node --import=tsx/esm --test test/*.ts`
